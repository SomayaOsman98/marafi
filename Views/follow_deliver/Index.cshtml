@model IEnumerable<marafi1.Models.follow_deliver>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/NewFolder1/Shared/_ThirdLayout.cshtml";
}

<p style="font-size:20px;">
    @Html.ActionLink(" إضافة جديد", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <label>كمية الشحنة</label>
        </th>
        <th>
            <label>نوع الشحنة</label>
        </th>
        <th>
            <label>قادمة من</label>
        </th>
        <th>
            <label>تاريخ الشحن</label>
        </th>
        <th>
            <label>المرسل</label>
        </th>
        <th>
            <label>المستلم</label>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.quantity_charg)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.type_charging)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coming_from)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sender)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.recip)
        </td>
        <td>
            @Html.ActionLink("تعديل", "Edit", new { id=item.Id_charging }) |
            @Html.ActionLink("تفاصيل", "Details", new { id=item.Id_charging }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.Id_charging })
        </td>
    </tr>
}

</table>
